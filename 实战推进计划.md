# trade-pulse 实战推进计划

> 协作者：你（决策）+ 我（编码/推送/复盘）
> 标的范围：588000（起步）→ 多 ETF → 跨市场套利

---

## 1. 总体路线图

```
                      ┌──────────────────────────────────────┐
                      │         阶段四：跨市场套利              │
                      │    A 股 vs 港股 vs 美股 ETF 价差      │
                      └──────────────┬───────────────────────┘
                                     │
                      ┌──────────────▼───────────────────────┐
                      │         阶段三：多标的并行              │
                      │    同一管线跑 N 只 ETF，定期扫描排名    │
                      └──────────────┬───────────────────────┘
                                     │
                      ┌──────────────▼───────────────────────┐
                      │         阶段二：单标的稳定运行            │
                      │    588000 全自动链路，攒真实数据        │
                      └──────────────┬───────────────────────┘
                                     │
                      ┌──────────────▼───────────────────────┐
                      │         阶段一：链路打通（现在）         │
                      │    编完剩余模块，让第一条信号跑起来      │
                      └──────────────────────────────────────┘
```

---

## 2. 阶段一：链路打通（M1，今天可完成）

### 目标

把当前代码变成一个**每天 14:30 自动出信号 + 推送到飞书**的闭环，先让唯一标的跑起来。

### 任务

#### M1.1 交易日历（新增）

```python
# tools/daily_pipeline/trading_calendar.py
def is_trading_day(date=None) -> bool
# 排除周末 + 查 AkShare 是否返回当日数据
```

- **为什么：** 14:30 跑脚本，如果不是交易日，AkShare 空数据→报错→你收不到推送且不知道原因
- **多标的预留：** is_trading_day() 不绑定标的，N 只共用同一份日历

#### M1.2 飞书推送（新增）

```python
# tools/daily_pipeline/feishu_push.py
def push_signal_card(result: dict, to: str = "群ID")
```

- **格式：** 富文本卡片

```
📊 trade-pulse | 2026-07-31
───────────────────────────
标的：588000 科创50ETF
决策：空仓
综合分：-0.78
仓位建议：0%

因子状态：
  动量     ██████░░░░  0.12
  趋势     ███████░░░  0.18
  量价     ░░░░░░░░░░ -0.30
  RSRS     ░░░░░░░░░░ -0.25
  比价     ░░░░░░░░░░ -0.35

一句话：各因子偏空，等待
```

- **鉴权：** 用 TOOLS.md 里的 WyrmGate 凭证（tenant_access_token）
- **多标的预留：** 推多个标的时为列表卡片，或逐条独立推送

#### M1.3 daily_panel.py 加 --push 参数

- 现有 `--json`、`--skip-fetch` 保持不变
- 新增 `--push`：算完后自动调用 feishu_push.py
- **不改现有逻辑：** 终端输出照旧，推送是添加

#### M1.4 代码整理提交

- git add 新增文件：backtest.py, parameter_search.py, data_provider/, strategies/, ui/, feishu_push.py, trading_calendar.py
- git add 修改文件：daily_pipeline 下的改动
- git commit + push 到 GitHub

#### M1.5 设定 cron

- **14:25** → `python daily_panel.py --push`
- 用 OpenClaw cron，精确到交易日

---

## 3. 阶段二：单标的稳定运行（M2 → 积累 1-2 周真实数据）

### 目标

让 588000 的信号链路**真正运转起来**，积累真实决策数据，验证系统在真实环境下的可靠性。

### 日常运行模式

```
14:25 — cron 触发，WSL 跑脚本
14:26 — fetch_data (带缓存，<3秒)
14:27 — compute_features (<1秒)
14:27 — signal_rules 决策 (<0.5秒)
14:28 — 飞书推送 (你收到卡片)
14:30-14:55 — 你看信号 + 行业判断 → 决策
14:55 — 操作（买入/卖出/不动）
```

### M2.1 交易记录就位

```bash
# data/trade_log.csv
日期,标的,操作,综合分,仓位%变化,入场价,出场价,盈亏,复盘
2026-07-31,588000,不动,-0.78,0%,—,—,—,
```

- 你操作后告诉我，我来记录
- **多标的预留：** `标的` 列已经存在

### M2.2 运营检查清单

| 检查项 | 频率 | 成功标准 |
|:---|:---:|:---:|
| 推送按时到 | 每交易日 | 14:30 ±5 分钟 |
| 数据有意义（非空/非0） | 每交易日 | 因子不全是 0 |
| 状态机正常（不抽风重置） | 每交易日 | 连续 2 天空仓不误变持仓 |
| 网络稳定（AkShare 不挂） | 每交易日 | fallback 切换<一次/周 |
| 你跟信号的直觉一致性 | 每交易日 | 记录你「同意/不同意」 |

### M2.3 这个阶段的目的

- 不是赚钱，是**证明系统可靠**——每天都能跑，不出错
- 让你形成习惯——14:30 扫一眼卡片，跟你的行业直觉对照
- 积累的交易记录是后续优化和扩标的的决策依据

---

## 4. 阶段三：多标的并行（M2 稳定后启动）

### 时机

M2 至少稳定 2 周信号 + 你有明确的第二个 ETF 目标。

### 架构改动

#### 核心变化：config 从单标的变多标的

```json
{
  "symbols": ["588000", "选择中..."],
  "default_benchmark": "000688",
  "markets": {
    "A": { "provider": "akshare", "calendar": "A-share" }
  }
}
```

#### daily_panel 改批量模式

```
输入: config.symbols → ["588000", "另一只ETF"]
输出:
┌────────────────────────────────┐
│  trade-pulse 多标的扫描        │
│  2026-07-31 (已收盘)           │
├────────────────────────────────┤
│  588000  科创50ETF             │
│  决策: 空仓   分数: -0.78     │
│  仓位: 0%   趋势: 偏空       │
├────────────────────────────────┤
│  另一只ETF  XXXETF             │
│  决策: 买入   分数: 0.45      │
│  仓位: 60%   趋势: 偏多      │
├────────────────────────────────┤
│  🏆 今日最优: 另一只ETF       │
└────────────────────────────────┘
```

#### 交易日历增强

不同市场不同日历：

```python
calendars = {
    "A": AShareCalendar(),
    "HK": HKCalendar(),    # 以后扩
    "US": USCalendar(),    # 以后扩
}
```

#### 回测 / 参数搜索增强

- 兼容多标的（需要为每个标的独立配置参数）
- 回测报告中按标的分组

### 选择第二只 ETF 的考量因素

| 因素 | 方向 |
|:---|:---|
| 与 588000 的相关性 | 低相关好（提供分散，套利空间） |
| 你熟悉的领域 | AI 产业链上下游（芯片/算力/应用） |
| 流动性 | 日均成交 > 1 亿 |
| 跨市场潜力 | 有无对应港/美股 ETF 可对照 |

---

## 5. 阶段四：跨市场套利（远期目标）

### 核心逻辑

同一资产在不同市场存在价差，信号系统在收盘后扫描多市场的相关标的，发现价差机会。

### 例子

```
588000（A 股科创50）vs 03187（港股科创50ETF）
某日收盘价差 > 2σ → 信号：套利窗口
```

### 需要的基建

| 能力 | 来源 | 优先级 |
|:---|:---|:---|
| 港股数据 | AkShare/AkShare4 | P1 |
| 美股数据 | yfinance | P2 |
| 价差计算 | 新增模块 | P2 |
| 历史价差统计 | 回测阶段积累数据 | P3 |

### 进入条件

- 阶段三稳定运行 ≥ 1 个月
- 至少有 2-3 只 A 股 ETF + 1-2 只对应港/美股 ETF 在跑
- 你确认想往这个方向投入时间和本金

---

## 6. 总时间线

```
阶段一：链路打通      ████░░░░░░░░░░░   今日
阶段二：单标稳定      ░░████████░░░░░   1-2 周
阶段三：多标的        ░░░░░░██████░░░   你选好第二只 ETF 后
阶段四：跨市场        ░░░░░░░░░░████   阶段三稳定后评估

当前所在：⬅️ 阶段一（00:00）
```

---

## 7. 马上需要你决策的事

1. **飞书推送到哪？** study-vault 群 / 私聊 / 新群？
2. **入金了吗？** 入了的话 M2 可能涉及真实交易
3. **14:25 cron 自动跑，接受吗？** 需要 mihomo 代理在 WSL 上当天在线

回了我就开始编码。
