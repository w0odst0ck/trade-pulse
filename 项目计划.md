# InstOrderTrace 项目计划 v3

> 标的：588000 科创50ETF | 本金：万元级 | 工具：证券账户（主动）+ 支付宝（定投）

---

## 项目架构

```
                      ┌─────────────────────────┐
                      │    最终决策（你）         │
                      │  规则信号 + Kronos + 行业信息│
                      └──────┬──────────────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
       ┌────────────┐ ┌────────────┐ ┌────────────┐
       │ 规则信号系统 │ │ Kronos 预测 │ │ 行业判断   │
       │ 状态机+多因子│ │ 分钟线→方向 │ │ AI 行业信息│
       │ 日线级     │ │ 辅助参考   │ │ 你的优势   │
       └────────────┘ └────────────┘ └────────────┘
              │              │
              ▼              ▼
       ┌─────────────────────────┐
       │     数据层              │
       │  588000 日线（免费）     │
       │  588000 分钟线（待购入）  │
       └─────────────────────────┘
```

---

## 阶段一：基建（证券账户开通后）

### 1.1 数据管道
- **日线**：AkShare 已验证可用（863 条近 3 年），待写 `fetch_data.py`
- **分钟线**：⛔ 暂缓，等纯规则实盘稳定再评估
- **格式**：`[日期, 开盘, 收盘, 最高, 最低, 成交量, 成交额, 振幅, 涨跌幅, 涨跌额, 换手率]`

### 1.2 信号规则系统（主模型 ✅ 设计已完成）
- **位置：** `tools/daily_pipeline/信号规则系统设计.md`
- **架构：** 周线大周期过滤 → 六因子连续打分 → 三级决策状态机 → 仓位映射
- **因子：** 短期动量(ROC5) / 中期趋势(MA20斜率) / 波动率(ATR分位) / 量价关系 / RSRS / 比价优势
- **状态机：** 空仓→持仓（即时切换）、持仓→空仓（2天确认防假穿）、持仓→观望（减半仓观察）
- **权重：** 动量 0.25 + 趋势 0.25 + 波动率 0.10 + 量价 0.15 + RSRS 0.15 + 比价 0.10
- **待编码：** fetch_data.py → compute_features.py → signal_rules.py → daily_panel.py

### 1.3 基础脚本（待编码）
- 数据获取模块：自动拉取 + 缓存 + 增量更新
- 日线特征：5/20/60 日均线、ROC、ATR、量价关系、RSRS
- 输出：每日信号面板

---

## 阶段二：Kronos 集成（分钟线数据到位后）

### 2.1 部署
- WSL2 本地运行 Kronos-mini（4.1M 参数，CPU 可跑）
- 输入：588000 分钟线 OHLCV
- 输出：未来 N 根 K 线预测方向（涨/跌/震荡概率）

### 2.2 交叉验证机制
```
规则信号 × Kronos 信号的 3×3 决策矩阵：

         Kronos 看涨 │ Kronos 震荡 │ Kronos 看跌
─────────┼───────────┼─────────────┼───────────
规则看多  │  重仓     │  轻仓      │  放弃
规则观望  │  试探     │  等待      │  等待
规则看空  │  观望     │  空仓      │  空仓
```

### 2.3 评估周期
- 每周一次对照：Kronos 预测 vs 实际走势
- 攒够 20 个信号后决定是否维持/调整权重

---

## 阶段三：实盘运行（证券账户入金后）

### 3.1 每日流程（5 分钟）
```
14:30 — 脚本自动出日线信号面板
14:40 — Kronos 跑一次分钟线预测（如果有数据）
14:45 — 你看面板 + 结合行业信息 → 做决策
14:55 — APP 挂单：买入/卖出/不动
```

### 3.2 仓位管理
```
重仓信号 → 60-80% 仓位
轻仓信号 → 20-40% 仓位
观望     → 0-20% 仓位
空仓     → 0
单笔止损 → -3%（硬止损）
连续3次亏损 → 暂停，复盘信号逻辑
```

### 3.3 交易记录
每笔必须记，模板：
```
日期:
操作: 买入/卖出/不动
触发信号: 规则(XX) / Kronos(XX) / 行业信息(XX)
入场:    出场:    盈亏:   仓位%:
复盘:
```

---

## 当前状态（2026-07-29）

| 项目 | 状态 |
|:----|:----:|
| 证券账户（东方财富） | ✅ 已开 |
| 588000 日线数据 | ✅ AkShare 验证可用（863 条近 3 年） |
| 规则信号系统 | ⏳ 设计完成，待编码（阶段一） |
| 分钟线数据（Kronos 用） | ⛔ 暂缓，等实盘稳定后再评估是否购入 |
| Kronos 部署 | ⛔ 依赖分钟线，同步暂缓 |
| 支付宝定投 | ✅ 继续 |

## 下一步触发点

1. ✅ ~~证券账户开通 → 阶段一启动~~
2. 📝 **目前位置：阶段一（基建）** — 编码：数据管道 → 特征工程 → 规则信号 → 每日面板
3. 📝 **阶段一完成后 → 阶段二（回测验证）：** 回测框架 → 参数优化 → 压力测试
4. ⛔ **分钟线数据：** 实盘稳定 1-2 月后再评估是否购入 → 部署 Kronos
5. 📝 **入金 → 模拟盘 → 实盘：** 参考 `项目目标.md` 中的阶段划分
